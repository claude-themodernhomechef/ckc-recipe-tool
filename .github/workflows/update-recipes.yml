# RETIRED — Phase 7 cleanup (2026-04-02)
#
# This workflow ran the Python pipeline: scrape.py → classify_protein.py → fetch_descriptions.py
# and committed recipes.json to the repo.
#
# It has been replaced by:
#   - Cloud Functions (functions/src/): enrichment fires automatically on recipe approval
#   - source-recipes.yml: agent writes new recipes directly to Firestore
#
# The workflow is disabled (no triggers). Kept for reference only.
# Original scripts are in archive/scripts/.

name: Update Recipes (RETIRED)

on:
  workflow_dispatch:    # manual-only; schedule removed

jobs:
  scrape:
    if: false           # never runs
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is retired."
