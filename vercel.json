{
  "buildCommand": "node scripts/generate-architecture-map.js && cd ckc-consumer-app && rm -rf node_modules && npm install && npx expo export --platform web",
  "outputDirectory": "ckc-consumer-app/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
