{
  "buildCommand": "cd ckc-consumer-app && npm install && npx expo export --platform web",
  "outputDirectory": "ckc-consumer-app/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
